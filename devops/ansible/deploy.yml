---
- name: This is a hello-world example
  hosts: all
  ansible_ssh_common_args: "-o StrictHostKeyChecking=no -i ${HOME}/.ssh/id_rsa"
  tasks:
    - name: Create a file called '/tmp/testfile.txt' with the content 'hello world'.
      copy:
        content: hello-world
        dest: /tmp/testfile.txt
        mode: 0644
